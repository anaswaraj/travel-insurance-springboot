# Common properties (keep shared config here)
# Environment-specific properties have been moved to application-dev.properties
# To enable the dev profile, set `SPRING_PROFILES_ACTIVE=dev` or use JVM arg:
#   -Dspring.profiles.active=dev

# Example to activate dev profile by default (un-comment to enable):
# spring.profiles.active=dev
